# Purging PATH Email

Power Automate Flow: Purging PATH Emil

Every Sunday at 11 PM, it automatically deletes the top 50 emails (normally would have 7 in the inbox) in the PATH folder in Outlook



# Purging Landing Folder

If no fallback needed (read today's file), delete any earlier files and keep today's file as tomorrow's fallback
If fallback needed (read yesterday's file), log warning and delete any files earlier than yesterday
